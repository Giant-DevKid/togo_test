
from conversation.handlers.event_organiser import (
    handle_event_name,
    # my_events_handler,
)

EVENT_ACTIONS = {
    "create_event": handle_event_name,
    # "my_events": my_events_handler,
}